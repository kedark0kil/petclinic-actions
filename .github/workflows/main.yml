on:
  push:
    branches: master
  
jobs:
  build:
    runs-on: ubuntu-latest
    name: build
    steps:
      - 
        name: build image
        uses: docker://docker
        run: |
          docker build -t petclinic-actions .
        #with:
        #  entrypoint:  docker

  #login:
  #  runs-on: ubuntu-latest
  #  name: login
  #  steps:
  #    - 
  #      name: login to docker
  #      uses: docker/login-action@v3
  #      with:
  #        registry: docker.io
  #        username: ${{ secrets.DOCKERHUB_USERNAME }}
  #        password: ${{ secrets.DOCKERHUB_TOKEN }}
          
